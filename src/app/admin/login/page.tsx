import {AdminLoginForm} from './login-form';import type {AdminLoginState} from '../auth-actions';export default async function AdminLogin({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;const initialError=['identifiants','acces','service'].includes(error??'')?error as AdminLoginState['error']:undefined;return <main className="auth-page"><section className="auth-card"><p className="section-kicker">Come & Eat</p><h1>Administration</h1><p>Connectez-vous avec votre compte autorisé.</p><AdminLoginForm initialError={initialError}/><small>Il n’existe aucune inscription administrateur publique.</small></section></main>}
