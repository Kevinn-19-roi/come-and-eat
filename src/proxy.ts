import {createServerClient} from '@supabase/ssr';import {NextResponse,type NextRequest} from 'next/server';
export async function proxy(request:NextRequest){const configuredUrl=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!configuredUrl||!key)return NextResponse.next();const url=configuredUrl.replace(/\/rest\/v1\/?$/,'');let response=NextResponse.next({request});const db=createServerClient(url,key,{cookies:{getAll:()=>request.cookies.getAll(),setAll(values){values.forEach(({name,value})=>request.cookies.set(name,value));response=NextResponse.next({request});values.forEach(({name,value,options})=>response.cookies.set(name,value,options))}}});const{data:{user}}=await db.auth.getUser();const path=request.nextUrl.pathname;if(path==='/admin/login'){if(user){const{data}=await db.from('profiles').select('role').eq('id',user.id).maybeSingle();if(data&&['admin','super_admin'].includes(data.role))return NextResponse.redirect(new URL('/admin',request.url))}return response}if(path.startsWith('/admin')){if(!user)return NextResponse.redirect(new URL('/admin/login',request.url));const{data}=await db.from('profiles').select('role').eq('id',user.id).maybeSingle();if(!data||!['admin','super_admin'].includes(data.role))return NextResponse.redirect(new URL('/admin/login?error=acces',request.url))}return response}
export const config={matcher:['/admin/:path*']};
