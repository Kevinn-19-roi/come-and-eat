'use client';
import { useState } from 'react';import type { Category,Product } from '@/types';import { ProductCard } from './product-card';
export function MenuCatalog({products,categories}:{products:Product[];categories:Category[]}){const [active,setActive]=useState('all');const shown=products.filter(p=>!p.archived&&(active==='all'||p.categoryId===active));return <><div className="filters"><button className={`pill ${active==='all'?'active':''}`} onClick={()=>setActive('all')}>Tout</button>{categories.filter(c=>c.visible).map(c=><button key={c.id} className={`pill ${active===c.id?'active':''}`} onClick={()=>setActive(c.id)}>{c.name}</button>)}</div>{shown.length?<div className="product-grid">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty card">Aucun produit dans cette catégorie.</div>}</>}
