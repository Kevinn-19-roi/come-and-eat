const formatPrice=(value:number)=>`${new Intl.NumberFormat('fr-FR').format(value)} FCFA`;
export function normalizeWhatsAppNumber(value:unknown){const raw=String(value??'').trim();let digits=raw.replace(/\D/g,'');if(digits.startsWith('00'))digits=digits.slice(2);else if(!raw.startsWith('+')&&digits.length===10&&digits.startsWith('0'))digits=`225${digits}`;return /^\d{8,15}$/.test(digits)?digits:''}
type Item={quantity:number;product_name:string;options?:Array<{name?:string}>};
export function buildRestaurantOrderWhatsAppLink(input:{number:unknown;restaurantName:unknown;reference:unknown;customerName:unknown;customerPhone:unknown;fulfillment:unknown;zone:unknown;address:unknown;instructions:unknown;items:Item[];amount:number;paymentStatus:unknown}){const number=normalizeWhatsAppNumber(input.number);if(!number)return'';const items=input.items.map(item=>`• ${item.quantity}x ${item.product_name}${item.options?.length?`\n  - ${item.options.map(option=>option.name).filter(Boolean).join('\n  - ')}`:''}`).join('\n\n');const payment=input.paymentStatus==='paid'?'Paiement vérifié par Come & Eat.':'Paiement à vérifier par Come & Eat.';const message=`Bonjour ${input.restaurantName||'restaurant'},\n\nVous avez une nouvelle commande Come & Eat ✅\n\nRéférence : ${input.reference}\n\nClient : ${input.customerName}\nTéléphone : ${input.customerPhone}\n\nMode : ${input.fulfillment==='delivery'?'Livraison':'Retrait'}\nZone : ${input.zone||'—'}\nAdresse : ${input.address||'—'}\nInstructions : ${input.instructions||'Aucune'}\n\nCommande :\n${items}\n\nMontant : ${formatPrice(input.amount)}\n\n${payment}\n\nMerci de consulter votre espace vendeur pour traiter la commande.`;return `https://wa.me/${number}?text=${encodeURIComponent(message)}`}
