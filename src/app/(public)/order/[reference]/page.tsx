import type {Metadata} from 'next';import {OrderStatus} from '@/components/order-status';export const metadata:Metadata={title:'Suivi de commande',robots:{index:false,follow:false}};export default async function Page({params,searchParams}:{params:Promise<{reference:string}>;searchParams:Promise<{token?:string}>}){const[{reference},{token}]=await Promise.all([params,searchParams]);return <><section className="page-hero"><div className="container"><p className="eyebrow">Suivi</p><h1 className="page-title">Votre commande</h1></div></section><OrderStatus reference={reference} token={token}/></>}
