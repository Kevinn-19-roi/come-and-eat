import Link from 'next/link';import {notFound} from 'next/navigation';import {AdminHeader} from '@/components/admin-ui';import {requireAdmin} from '@/lib/auth/admin';import {getAdminRestaurant,operationLabels,orderLabels,validationLabels} from '@/services/admin-marketplace';import {getCuisineTypes} from '@/services/vendor-dashboard';import {saveRestaurantMember,setRestaurantState,updateRestaurantAdmin} from '@/app/admin/actions';import {formatPrice} from '@/lib/utils';
const days=['Dimanche','Lundi','Mardi','Mercredi','Jeudi','Vendredi','Samedi'];
export default async function Page({params}:{params:Promise<{id:string}>}){await requireAdmin();const{id}=await params;const[details,cuisines]=await Promise.all([getAdminRestaurant(id),getCuisineTypes()]);if(!details)notFound();const{restaurant}=details;return <><AdminHeader eyebrow="Restaurant" title={restaurant.name} action={<Link className="btn btn-outline" href="/admin/restaurants">← Restaurants</Link>}/><div className="admin-state-bar"><span className="vendor-badge">{validationLabels[restaurant.validation_status]}</span><span className="vendor-badge">{operationLabels[restaurant.operating_status]}</span>{!restaurant.is_official?<><form action={setRestaurantState}><input type="hidden" name="id" value={id}/><button className="btn btn-danger" name="action" value="suspend">Suspendre</button></form><form action={setRestaurantState}><input type="hidden" name="id" value={id}/><button className="btn btn-outline" name="action" value="approve">Réactiver</button></form><form action={setRestaurantState}><input type="hidden" name="id" value={id}/><button className="btn btn-outline" name="action" value={restaurant.operating_status==='paused'?'open':'pause'}>{restaurant.operating_status==='paused'?'Reprendre les commandes':'Mettre en pause'}</button></form></>:null}</div><div className="admin-detail-grid"><form action={updateRestaurantAdmin} className="admin-card vendor-form"><input type="hidden" name="id" value={id}/><h2>Informations du restaurant</h2><div className="vendor-form-grid"><label className="field full"><span>Nom</span><input name="name" defaultValue={restaurant.name} required/></label><label className="field full"><span>Description</span><textarea name="description" rows={4} defaultValue={restaurant.description}/></label><label className="field"><span>Téléphone</span><input name="phone" defaultValue={restaurant.phone??''}/></label><label className="field"><span>WhatsApp</span><input name="whatsapp" defaultValue={restaurant.whatsapp??''}/></label><label className="field full"><span>Email</span><input type="email" name="email" defaultValue={restaurant.email??''}/></label><label className="field full"><span>Adresse</span><input name="address" defaultValue={restaurant.address??''}/></label><label className="field"><span>Commune</span><input name="commune" defaultValue={restaurant.commune??''}/></label><label className="field"><span>Lien Maps</span><input type="url" name="maps_url" defaultValue={restaurant.maps_url??''}/></label><label className="field"><span>Délai de préparation</span><input type="number" name="average_prep_minutes" defaultValue={restaurant.average_prep_minutes}/></label><label className="field"><span>Ouverture</span><select name="operating_status" defaultValue={restaurant.operating_status}><option value="open">Ouvert</option><option value="closed">Fermé</option><option value="paused">En pause</option></select></label><label className="field"><span>Validation</span><select name="validation_status" defaultValue={restaurant.validation_status}><option value="draft">Brouillon</option><option value="pending_review">À valider</option><option value="approved">Validé</option><option value="rejected">Refusé</option><option value="suspended">Suspendu</option></select></label></div><fieldset className="cuisine-choices"><legend>Types de cuisine</legend>{cuisines.map(c=><label key={c.id}><input type="checkbox" name="cuisine_ids" value={c.id} defaultChecked={details.cuisineIds.includes(c.id)}/>{c.name}</label>)}</fieldset><div className="option-checks"><label><input type="checkbox" name="delivery_available" defaultChecked={restaurant.delivery_available}/>Livraison</label><label><input type="checkbox" name="pickup_available" defaultChecked={restaurant.pickup_available}/>Retrait</label></div><button className="btn btn-dark">Enregistrer</button></form><aside className="admin-detail-side"><section className="admin-card"><h2>Membres</h2>{details.members.map(member=>{const profile=Array.isArray(member.profile)?member.profile[0]:member.profile;return <form action={saveRestaurantMember} className="member-row" key={member.user_id}><input type="hidden" name="restaurant_id" value={id}/><input type="hidden" name="user_id" value={member.user_id}/><span><strong>{profile?.display_name||'Utilisateur'}</strong><small>{profile?.phone||'Sans téléphone'}</small></span><select name="role" defaultValue={member.role}><option value="owner">Propriétaire</option><option value="manager">Responsable</option><option value="staff">Équipe</option></select><button>Enregistrer</button><button className="danger-link" name="action" value="remove">Retirer</button></form>})}{!details.members.length?<p className="muted">Aucun membre associé.</p>:null}</section><section className="admin-card"><h2>Horaires</h2>{details.hours.map(hour=><p key={hour.id}>{days[hour.day_of_week]} <strong>{hour.is_closed?'Fermé':`${hour.opens_at.slice(0,5)} – ${hour.closes_at.slice(0,5)}`}</strong></p>)}</section></aside></div><section className="admin-card"><div className="card-head"><h2>Produits récents</h2><Link href={`/admin/products?restaurant=${id}`}>Voir tous</Link></div>{details.products.map(product=><div className="admin-list-row" key={product.id}><span><strong>{product.name}</strong><small>{product.hidden_by_admin?'Masqué':product.availability?'Disponible':'Indisponible'}</small></span><strong>{formatPrice(product.base_price)}</strong></div>)}{!details.products.length?<p className="muted">Aucun produit.</p>:null}</section><section className="admin-card"><div className="card-head"><h2>Commandes récentes</h2><Link href={`/admin/orders?restaurant=${id}`}>Voir toutes</Link></div>{details.orders.map(order=>{const global=Array.isArray(order.order)?order.order[0]:order.order;return <Link className="admin-list-row" key={order.id} href={global?.id?`/admin/orders/${global.id}`:'/admin/orders'}><span><strong>{global?.reference}</strong><small>{global?.customer_name}</small></span><span>{orderLabels[order.status]} · {formatPrice(order.subtotal)}</span></Link>})}{!details.orders.length?<p className="muted">Aucune commande.</p>:null}</section></>}
