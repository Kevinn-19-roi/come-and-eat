import {notFound} from 'next/navigation';import {requireAdmin} from '@/lib/auth/admin';import {getOfficialStoreDetails} from '@/services/official-store';import {getVendorCategories,getVendorProduct} from '@/services/vendor-dashboard';import {VendorProductForm} from '@/components/vendor-product-form';import type{EditableOptionGroup}from '@/components/vendor-forms';import {updateOfficialProduct} from '../../../actions';
export default async function Page({params}:{params:Promise<{id:string}>}){await requireAdmin();const[{id},details,categories]=await Promise.all([params,getOfficialStoreDetails(),getVendorCategories()]);const product=await getVendorProduct(details.store.id,id);if(!product)notFound();const options=(product.links??[]).flatMap(link=>{const group=link.group as unknown as{type:string;is_required:boolean;max_choices:number|null;options:Array<{name:string;price_delta:number}>}|null;if(!group||!['accompaniment','drink','supplement'].includes(group.type))return[];return[{type:group.type,required:group.is_required,multiple:group.max_choices===null||group.max_choices>1,options:(group.options??[]).map(option=>({name:option.name,price:option.price_delta}))}as EditableOptionGroup]});return <><div className="admin-page-head"><div><p className="eyebrow">Boutique officielle</p><h1>Modifier {product.name}</h1></div></div><VendorProductForm action={updateOfficialProduct.bind(null,id)} categories={categories} media={details.media} restaurantId={details.store.id} product={product} options={options}/></>}
