import Image from 'next/image';import Link from 'next/link';import {requireAdmin} from '@/lib/auth/admin';import {getOfficialStoreDetails} from '@/services/official-store';import {VendorMediaUpload} from '@/components/vendor-media-library';
export default async function Page(){await requireAdmin();const{store,media}=await getOfficialStoreDetails();return <><div className="admin-page-head"><div><p className="eyebrow">Boutique officielle</p><h1>Photos</h1></div><div className="store-admin-actions"><Link className="btn btn-outline" href="/admin/store">Retour</Link><VendorMediaUpload restaurantId={store.id}/></div></div>{media.length?<div className="vendor-media-grid">{media.map(item=><article key={item.id}><span><Image src={item.publicUrl} alt={item.alt_text||item.file_name} fill sizes="240px"/></span><div><strong>{item.file_name}</strong><small>{item.type}</small></div></article>)}</div>:<div className="admin-card empty"><h2>Aucune photo</h2><p>Ajoutez les visuels de la boutique officielle.</p></div>}</>}
