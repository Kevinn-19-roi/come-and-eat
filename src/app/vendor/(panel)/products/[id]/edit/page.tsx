import {notFound} from 'next/navigation';import {requireVendorManager} from '@/lib/auth/vendor';import {updateProduct} from '@/app/vendor/actions';import {getVendorCategories,getVendorMedia,getVendorProduct} from '@/services/vendor-dashboard';import {VendorProductForm} from '@/components/vendor-product-form';import type {EditableOptionGroup} from '@/components/vendor-forms';
export default async function EditProductPage({params}:{params:Promise<{id:string}>}){const[{id},context]=await Promise.all([params,requireVendorManager()]);const[product,categories,media]=await Promise.all([getVendorProduct(context.restaurant!.id,id),getVendorCategories(),getVendorMedia(context.userId,context.restaurant!.id)]);if(!product)notFound();const options=(product.links??[]).flatMap(link=>{const group=link.group as unknown as {type:string;is_required:boolean;max_choices:number|null;options:Array<{name:string;price_delta:number}>}|null;if(!group||!['accompaniment','drink','supplement'].includes(group.type))return[];return[{type:group.type,required:group.is_required,multiple:group.max_choices===null||group.max_choices>1,options:(group.options??[]).map(option=>({name:option.name,price:option.price_delta}))} as EditableOptionGroup]});const action=updateProduct.bind(null,id);return <><div className="vendor-page-head"><div><p className="section-kicker">Votre carte</p><h1>Modifier {product.name}</h1><p>Le SKU et l’adresse de la page restent gérés automatiquement.</p></div></div>{product.hidden_by_admin?<div className="vendor-alert danger"><strong>Masqué par Come & Eat</strong><p>{product.moderation_note||'Contactez l’administration avant de remettre ce produit en ligne.'}</p></div>:null}<VendorProductForm action={action} categories={categories} media={media} restaurantId={context.restaurant!.id} product={product} options={options}/></>}
