import Link from 'next/link';
import {AdminHeader} from '@/components/admin-ui';
import {saveCommissionSetting,saveSiteSetting} from '@/app/admin/actions';
import {requireAdmin} from '@/lib/auth/admin';
import {getAdminContent} from '@/services/admin-marketplace';
export default async function Page({searchParams}:{searchParams:Promise<{saved?:string}>}){await requireAdmin();const[{saved},{settings}]=await Promise.all([searchParams,getAdminContent()]);const platform=(settings.find(row=>row.key==='platform')?.value??{}) as Record<string,unknown>,commission=(settings.find(row=>row.key==='marketplace_commission')?.value??{}) as Record<string,unknown>,rate=Number(commission.rate_bps??1000)/100;return <><AdminHeader eyebrow="Réglages essentiels" title="Paramètres"/>{saved==='commission'?<div className="form-feedback success">Commission enregistrée. Les anciennes commandes restent inchangées.</div>:null}<div className="admin-two"><div><form action={saveSiteSetting} className="admin-card vendor-form"><input type="hidden" name="key" value="platform"/><h2>Prise de commandes</h2><input type="hidden" name="ordering_enabled" value="false"/><label className="check-row"><input type="checkbox" name="ordering_enabled" defaultChecked={platform.ordering_enabled!=='false'}/>La plateforme accepte les commandes</label><input type="hidden" name="delivery_enabled" value="false"/><label className="check-row"><input type="checkbox" name="delivery_enabled" defaultChecked={platform.delivery_enabled!=='false'}/>Livraison proposée</label><input type="hidden" name="pickup_enabled" value="false"/><label className="check-row"><input type="checkbox" name="pickup_enabled" defaultChecked={platform.pickup_enabled!=='false'}/>Retrait proposé</label><button className="btn btn-dark">Enregistrer</button></form><form action={saveCommissionSetting} className="admin-card vendor-form"><h2>Paiement</h2><label className="field"><span>Commission Come & Eat</span><div className="commission-input"><input type="number" name="commission_rate" min="0" max="100" step="0.01" defaultValue={rate}/><strong>%</strong></div><small>Ce taux est figé sur chaque nouvelle commande. La boutique officielle conserve une commission de 0 %.</small></label><button className="btn btn-dark">Enregistrer la commission</button></form></div><section className="admin-card"><h2>Informations commerciales</h2><p className="muted">Le téléphone, l’adresse, les horaires et les liens publics sont regroupés dans le contenu du site. Les secrets techniques ne sont jamais affichés ici.</p><Link className="btn btn-outline" href="/admin/content">Modifier le contenu</Link></section></div></>}
