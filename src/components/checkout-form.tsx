'use client';
import Link from 'next/link';import {FormEvent,useRef,useState} from 'react';import {useRouter} from 'next/navigation';import {useCart} from './cart-provider';import {formatPrice} from '@/lib/utils';import type {DeliveryZone} from '@/types';import {placeOrder} from '@/app/(public)/checkout/actions';
export function CheckoutForm({zones}:{zones:DeliveryZone[]}){const cart=useCart();const router=useRouter();const key=useRef<string>(crypto.randomUUID());const[fulfillment,setFulfillment]=useState<'delivery'|'pickup'>('delivery');const[zone,setZone]=useState(zones[0]?.id??'');const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[promo,setPromo]=useState('');const restaurantCount=Math.max(1,new Set(cart.items.map(i=>i.restaurantId).filter(Boolean)).size);const fee=fulfillment==='delivery'?(zones.find(z=>z.id===zone)?.fee??0)*restaurantCount:0;
const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!cart.items.length||loading)return;setLoading(true);setError('');const form=new FormData(e.currentTarget);const result=await placeOrder({customerName:String(form.get('name')??''),customerPhone:String(form.get('phone')??''),customerEmail:String(form.get('email')??''),fulfillment,address:String(form.get('address')??''),commune:String(form.get('commune')??''),note:String(form.get('instructions')??''),wantsCutlery:form.get('cutlery')==='on',deliveryZoneId:fulfillment==='delivery'?zone:undefined,promotionCode:promo,idempotencyKey:key.current,items:cart.items.map(item=>({productId:item.productId,quantity:item.quantity,optionIds:item.selections.map(s=>s.optionId)}))});if(!result.ok){setError(result.error);setLoading(false);return}sessionStorage.setItem(`come-eat-order-${result.order.reference}`,JSON.stringify({paymentUrl:result.payment.url,merchantPhone:result.payment.merchantPhone}));cart.clear();router.push(`/order/${result.order.reference}?token=${result.order.tracking_token}`)};
if(!cart.items.length)return <div className="store-container"><div className="empty-state"><h2>Votre panier est vide</h2><Link className="btn btn-orange" href="/menu">Retour au menu</Link></div></div>;
return <form onSubmit={submit} className="store-container checkout-grid"><div className="checkout-flow"><section className="checkout-card"><div className="step-title"><span>1</span><div><small>Vos coordonnées</small><h2>Comment vous joindre ?</h2></div></div><div className="form-grid"><div className="field"><label htmlFor="name">Nom complet</label><input name="name" id="name" required autoComplete="name"/></div><div className="field"><label htmlFor="phone">Téléphone</label><input name="phone" id="phone" required type="tel" autoComplete="tel" placeholder="+225 07…"/></div><div className="field full"><label htmlFor="email">Email <small>(facultatif)</small></label><input name="email" id="email" type="email" autoComplete="email"/></div></div></section><section className="checkout-card"><div className="step-title"><span>2</span><div><small>Mode de réception</small><h2>Livraison ou retrait ?</h2></div></div><div className="choice-grid"><label className={fulfillment==='delivery'?'choice-card selected':'choice-card'}><input type="radio" checked={fulfillment==='delivery'} onChange={()=>setFulfillment('delivery')}/><b>Livraison</b><small>À l’adresse indiquée</small></label><label className={fulfillment==='pickup'?'choice-card selected':'choice-card'}><input type="radio" checked={fulfillment==='pickup'} onChange={()=>setFulfillment('pickup')}/><b>Retrait</b><small>Directement au restaurant</small></label></div>{fulfillment==='delivery'?<div className="form-grid delivery-fields"><div className="field full"><label htmlFor="address">Adresse et repère</label><input name="address" id="address" required autoComplete="street-address"/></div><div className="field"><label htmlFor="commune">Commune</label><input name="commune" id="commune" required/></div><div className="field"><label htmlFor="zone">Zone de livraison</label><select id="zone" value={zone} onChange={e=>setZone(e.target.value)} required>{zones.map(z=><option key={z.id} value={z.id}>{z.name} — {formatPrice(z.fee)}</option>)}</select></div></div>:null}<div className="field"><label htmlFor="instructions">Une précision ? <small>(facultatif)</small></label><textarea name="instructions" id="instructions" rows={3}/></div><label className="cutlery"><input name="cutlery" type="checkbox"/><span>Ajouter des couverts</span></label></section><section className="checkout-card"><div className="step-title"><span>3</span><div><small>Paiement</small><h2>Payer avec Wave</h2></div></div><div className="payment-grid"><label className="selected"><input type="radio" defaultChecked/><b>Wave</b><small>Vous serez guidé après la création de la commande.</small></label></div><p className="simulation-note">La commande reste en attente tant que le paiement n’est pas confirmé de façon vérifiable.</p></section></div><aside className="order-summary checkout-summary"><span className="summary-eyebrow">Votre commande</span><h2>Récapitulatif</h2>{cart.items.map(i=><div className="checkout-line" key={i.id}><span><b>{i.quantity} × {i.name}</b><small>{i.selections.map(s=>s.name).join(', ')}</small></span><strong>{formatPrice((i.unitPrice+i.selections.reduce((s,o)=>s+o.price,0))*i.quantity)}</strong></div>)}<div className="field"><label htmlFor="promo">Code promo</label><input id="promo" value={promo} onChange={e=>setPromo(e.target.value)}/></div><div className="summary-row"><span>Sous-total indicatif</span><strong>{formatPrice(cart.subtotal)}</strong></div><div className="summary-row"><span>Livraison indicative</span><strong>{formatPrice(fee)}</strong></div><div className="summary-row total"><strong>Total indicatif</strong><strong>{formatPrice(cart.subtotal+fee)}</strong></div><p className="muted">Le montant final est recalculé par le serveur.</p>{error?<p className="form-error" role="alert">{error}</p>:null}<button className="btn btn-orange summary-cta" disabled={loading}>{loading?'Vérification en cours…':'Créer et payer ma commande'}</button></aside></form>}
