'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';import {useState} from 'react';
import {adminLogout} from '@/app/admin/auth-actions';
const links=[['/admin','▦','Tableau de bord'],['/admin/orders','◴','Commandes'],['/admin/products','□','Produits'],['/admin/categories','◇','Catégories'],['/admin/options','⌘','Options & suppléments'],['/admin/promotions','%','Promotions'],['/admin/delivery','⌖','Livraison'],['/admin/media','▧','Galerie'],['/admin/messages','✉','Messages'],['/admin/content','✎','Contenu du site'],['/admin/settings','⚙','Paramètres']];
export function AdminShell({children}:{children:React.ReactNode}){const path=usePathname();const[open,setOpen]=useState(false);if(path==='/admin/login')return <>{children}</>;return <div className="admin-app"><aside className={open?'admin-sidebar open':'admin-sidebar'}><div className="admin-logo"><span>Come</span> & Eat<small>Administration</small></div><nav>{links.map(([href,icon,label])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={path===href?'active':''}><i>{icon}</i>{label}</Link>)}</nav><Link href="/" className="back-site">← Voir le site public</Link></aside>{open?<button className="sidebar-backdrop" onClick={()=>setOpen(false)} aria-label="Fermer le menu"/>:null}<div className="admin-main"><header className="admin-top"><button className="admin-menu-btn" onClick={()=>setOpen(true)} aria-label="Ouvrir la navigation">☰</button><div><strong>Administration Come & Eat</strong><span>Données sécurisées</span></div><form action={adminLogout} className="admin-logout"><button type="submit" title="Se déconnecter">Déconnexion</button></form></header><main className="admin-content">{children}</main></div></div>}
