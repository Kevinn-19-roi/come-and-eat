import type { Metadata } from 'next';
import { MenuCatalog } from '@/components/menu-catalog';
import {catalogRepository} from '@/services/repositories';
export const metadata:Metadata={title:'Menu & Carte'};
export default async function MenuPage({searchParams}:{searchParams:Promise<{category?:string}>}){const[products,categories,params]=await Promise.all([catalogRepository.getProducts(),catalogRepository.getCategories(),searchParams]);const selected=categories.find(category=>category.slug===params.category)?.id??'all';return <><section className="menu-hero"><div className="store-container"><p className="section-kicker">🍽 Préparé minute, rien que pour vous</p><h1>Menu & carte gourmande</h1><p>Explorez nos recettes maison, personnalisez votre plat et commandez en quelques clics.</p><div className="menu-facts"><span>♧ Produits frais</span><span>◷ Prêt en 10–25 min</span><span>♢ Paiement flexible</span></div></div></section><section className="section menu-section"><div className="store-container"><MenuCatalog products={products} categories={categories} initialActive={selected}/></div></section></>}
