'use client';
import Link from 'next/link'; import { useState } from 'react'; import { useCart } from './cart-provider'; import { CartIcon, MenuIcon } from './icons';
export function PublicHeader(){const [open,setOpen]=useState(false);const {count}=useCart();return <header className="site-header"><div className="container nav-wrap"><Link href="/" className="brand"><span>Come</span> & Eat</Link><button className="mobile-menu" onClick={()=>setOpen(!open)} aria-label="Ouvrir le menu"><MenuIcon/></button><nav className={open?'nav-links open':'nav-links'} onClick={()=>setOpen(false)}><Link href="/menu">Le menu</Link><Link href="/about">Notre histoire</Link><Link href="/contact">Contact</Link></nav><Link href="/cart" className="cart-link"><CartIcon/><span>Panier</span>{count>0&&<b>{count}</b>}</Link></div></header>}
