import {AdminHeader} from '@/components/admin-ui';import {savePromotionAdmin,togglePromotion} from '@/app/admin/actions';import {requireAdmin} from '@/lib/auth/admin';import {formatPrice} from '@/lib/utils';import {getAdminCategories,getAdminProducts,getAdminPromotions,getAdminRestaurants} from '@/services/admin-marketplace';
export default async function Page(){await requireAdmin();const[rows,restaurants,products,categories]=await Promise.all([getAdminPromotions(),getAdminRestaurants(),getAdminProducts({}),getAdminCategories()]);return <><AdminHeader eyebrow="Offres de la plateforme" title="Promotions"/><div className="admin-two"><section className="admin-card"><h2>Promotions actuelles</h2>{rows.map(row=>{const restaurant=Array.isArray(row.restaurant)?row.restaurant[0]:row.restaurant;const product=Array.isArray(row.product)?row.product[0]:row.product;const category=Array.isArray(row.category)?row.category[0]:row.category;return <div className="admin-list-row" key={row.id}><span><strong>{row.name}</strong><small>{row.discount_type==='percent'?`${row.value} %`:formatPrice(row.value)} · {product?.name||category?.name||restaurant?.name||'Toute la plateforme'}</small></span><form action={togglePromotion} className="table-actions"><input type="hidden" name="id" value={row.id}/><button name="action" value={row.is_active?'deactivate':'activate'}>{row.is_active?'Désactiver':'Activer'}</button><button className="danger-link" name="action" value="archive">Archiver</button></form></div>})}{!rows.length?<div className="admin-empty"><strong>Aucune promotion</strong></div>:null}</section><form action={savePromotionAdmin} className="admin-card vendor-form"><h2>Créer une promotion</h2><label className="field"><span>Nom</span><input name="name" required/></label><div className="vendor-form-grid"><label className="field"><span>Réduction</span><select name="discount_type"><option value="percent">Pourcentage</option><option value="fixed">Montant fixe</option></select></label><label className="field"><span>Valeur</span><input type="number" min="0" name="value" required/></label></div><label className="field"><span>Restaurant (facultatif)</span><select name="restaurant_id"><option value="">Toute la plateforme</option>{restaurants.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></label><label className="field"><span>Produit (facultatif)</span><select name="product_id"><option value="">Aucun produit précis</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="field"><span>Catégorie (facultatif)</span><select name="category_id"><option value="">Aucune catégorie précise</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><div className="vendor-form-grid"><label className="field"><span>Début</span><input type="datetime-local" name="starts_at"/></label><label className="field"><span>Fin</span><input type="datetime-local" name="ends_at"/></label></div><label className="field"><span>Code facultatif</span><input name="code"/></label><label className="check-row"><input type="checkbox" name="is_active" defaultChecked/>Active</label><button className="btn btn-dark">Créer</button></form></div></>}
