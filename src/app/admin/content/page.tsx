import {AdminHeader} from '@/components/admin-ui';import {saveHomepageSection,saveSiteSetting} from '@/app/admin/actions';import {requireAdmin} from '@/lib/auth/admin';import {getAdminContent} from '@/services/admin-marketplace';
const fields:Record<string,{label:string;names:{key:string;label:string}[]}>={brand:{label:'Identité',names:[{key:'name',label:'Nom de la marque'},{key:'tagline',label:'Slogan'}]},announcement:{label:'Bandeau supérieur',names:[{key:'text',label:'Texte du bandeau'}]},contact:{label:'Coordonnées',names:[{key:'phone',label:'Téléphone'},{key:'email',label:'Email'},{key:'address',label:'Adresse'}]},hours:{label:'Horaires généraux',names:[{key:'label',label:'Horaires affichés'}]},footer:{label:'Pied de page',names:[{key:'description',label:'Description'},{key:'legal',label:'Mention légale'}]},external_links:{label:'Liens externes',names:[{key:'bloop',label:'Site Bloop'}]}};
export default async function Page(){await requireAdmin();const{settings,sections}=await getAdminContent();const values=Object.fromEntries(settings.map(s=>[s.key,s.value as Record<string,unknown>]));return <><AdminHeader eyebrow="Textes administrables" title="Contenu du site"/><p className="admin-notice">Ces valeurs remplacent la configuration locale. Si une valeur manque, le site conserve automatiquement son texte de secours.</p><div className="admin-content-grid">{Object.entries(fields).map(([key,group])=><form action={saveSiteSetting} className="admin-card vendor-form" key={key}><input type="hidden" name="key" value={key}/><h2>{group.label}</h2>{group.names.map(field=><label className="field" key={field.key}><span>{field.label}</span><input name={field.key} defaultValue={String(values[key]?.[field.key]??'')}/></label>)}<button className="btn btn-dark">Enregistrer</button></form>)}</div><AdminHeader eyebrow="Accueil" title="Sections éditoriales"/><section className="admin-content-grid">{sections.map(section=><form action={saveHomepageSection} className="admin-card vendor-form" key={section.id}><input type="hidden" name="id" value={section.id}/><input type="hidden" name="section_key" value={section.section_key}/><label className="field"><span>Titre</span><input name="title" defaultValue={section.title??''}/></label><label className="field"><span>Sous-titre</span><input name="subtitle" defaultValue={section.subtitle??''}/></label><label className="field"><span>Texte</span><textarea name="body" defaultValue={section.body??''}/></label><label className="field"><span>Ordre</span><input name="sort_order" type="number" defaultValue={section.sort_order}/></label><label className="check-row"><input type="checkbox" name="is_visible" defaultChecked={section.is_visible}/>Afficher cette section</label><button className="btn btn-dark">Enregistrer</button></form>)}</section></>}
