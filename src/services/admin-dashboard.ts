import 'server-only';import {createClient} from '@/lib/supabase/server';
export type AdminRecentOrder={id:string;reference:string;customerName:string;fulfillment:'delivery'|'pickup';total:number;paymentStatus:string;createdAt:string};export type AdminDashboardData={ordersToday:number;revenueToday:number;pendingOrders:number;preparingOrders:number;unavailableProducts:number;pendingRestaurants:number;pendingApplications:number;recentOrders:AdminRecentOrder[]};
function logQueryError(query:string,error:{code?:string}|null){if(error)console.error('[admin-dashboard] query_failed',{query,code:error.code})}
export async function getAdminDashboardData():Promise<AdminDashboardData>{const db=await createClient();const start=new Date();start.setUTCHours(0,0,0,0);const[ordersResult,restaurantOrdersResult,productsResult,restaurantsResult,applicationsResult]=await Promise.all([db.from('orders').select('id,reference,customer_name,fulfillment,total,payment_status,created_at').gte('created_at',start.toISOString()).order('created_at',{ascending:false}).limit(8),db.from('restaurant_orders').select('status').in('status',['pending','confirmed','preparing']),db.from('products').select('*',{count:'exact',head:true}).eq('availability',false).eq('is_archived',false),db.from('restaurants').select('*',{count:'exact',head:true}).eq('validation_status','pending_review'),db.from('seller_applications').select('*',{count:'exact',head:true}).in('status',['submitted','under_review'])]);logQueryError('orders_today',ordersResult.error);logQueryError('restaurant_orders',restaurantOrdersResult.error);logQueryError('unavailable_products',productsResult.error);logQueryError('pending_restaurants',restaurantsResult.error);logQueryError('pending_applications',applicationsResult.error);const orders=ordersResult.data??[];const operational=restaurantOrdersResult.data??[];return{ordersToday:orders.length,revenueToday:orders.reduce((sum,row)=>sum+(row.total??0),0),pendingOrders:operational.filter(row=>row.status==='pending'||row.status==='confirmed').length,preparingOrders:operational.filter(row=>row.status==='preparing').length,unavailableProducts:productsResult.count??0,pendingRestaurants:restaurantsResult.count??0,pendingApplications:applicationsResult.count??0,recentOrders:orders.map(row=>({id:row.id,reference:row.reference,customerName:row.customer_name??'Client',fulfillment:row.fulfillment,total:row.total??0,paymentStatus:row.payment_status,createdAt:row.created_at}))}}
