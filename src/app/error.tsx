'use client';
import {useEffect} from 'react';export default function ErrorPage({error,retry}:{error:Error&{digest?:string};retry:()=>void}){useEffect(()=>{console.error('[app-error]',{digest:error.digest})},[error]);return <div className="store-container"><div className="empty-state"><span>!</span><h1>Une petite erreur s’est glissée en cuisine</h1><p>Veuillez réessayer dans un instant.</p><button className="btn btn-orange" onClick={retry}>Réessayer</button></div></div>}
