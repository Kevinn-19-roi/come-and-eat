import type { Metadata } from 'next';import { CheckoutForm } from '@/components/checkout-form';import { localSettingsRepository } from '@/services/repositories';export const metadata:Metadata={title:'Finaliser la commande'};export default async function Page(){const zones=await localSettingsRepository.getDeliveryZones();return <><section className="page-hero"><div className="container"><p className="eyebrow">Dernière étape</p><h1 className="page-title">Finaliser la commande</h1></div></section><CheckoutForm zones={zones}/></>}
