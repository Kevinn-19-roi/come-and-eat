import {notFound} from 'next/navigation';import type {Metadata} from 'next';import {ProductDetail} from '@/components/product-detail';import {catalogRepository} from '@/services/repositories';
export async function generateStaticParams(){return(await catalogRepository.getProducts()).map(product=>({slug:product.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const product=await catalogRepository.getProductBySlug(slug);return{title:product?.name??'Produit'}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const product=await catalogRepository.getProductBySlug(slug);if(!product)notFound();const groups=await catalogRepository.getOptionGroups(product.id);return <ProductDetail product={product} groups={groups}/>}
