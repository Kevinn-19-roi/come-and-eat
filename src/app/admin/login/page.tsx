import {AdminLoginForm} from './login-form';import type {AdminLoginState,AdminSupportCode} from '../auth-actions';export default async function AdminLogin({searchParams}:{searchParams:Promise<{error?:string;code?:string}>}){const{error,code}=await searchParams;const initialError=['identifiants','acces','service'].includes(error??'')?error as AdminLoginState['error']:undefined;const initialSupportCode=['AUTH-URL-MISSING','AUTH-KEY-MISSING'].includes(code??'')?code as AdminSupportCode:undefined;return <main className="auth-page"><section className="auth-card"><p className="section-kicker">Come & Eat</p><h1>Administration</h1><p>Connectez-vous avec votre compte autorisé.</p><AdminLoginForm initialError={initialError} initialSupportCode={initialSupportCode}/><small>Il n’existe aucune inscription administrateur publique.</small></section></main>}
