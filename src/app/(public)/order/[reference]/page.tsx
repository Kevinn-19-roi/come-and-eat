import type { Metadata } from 'next';import { OrderStatus } from '@/components/order-status';export const metadata:Metadata={title:'Suivi de commande'};export default async function Page({params}:{params:Promise<{reference:string}>}){const {reference}=await params;return <><section className="page-hero"><div className="container"><p className="eyebrow">Suivi</p><h1 className="page-title">Votre commande</h1></div></section><OrderStatus reference={reference}/></>}
