import type {Metadata} from 'next';import {CheckoutForm} from '@/components/checkout-form';import {settingsRepository} from '@/services/repositories';export const metadata:Metadata={title:'Finaliser la commande'};export default async function Page(){const zones=await settingsRepository.getDeliveryZones();return <><section className="inner-hero checkout-hero"><div className="store-container"><p className="section-kicker">✓ Simple, rapide et sécurisé</p><h1>Finaliser ma commande</h1><p>Plus que quelques informations avant de passer à table.</p></div></section><CheckoutForm zones={zones}/></>}
